# Email Configuration
# Replace these with your actual Gmail credentials
EMAIL_USER=your-gmail@gmail.com
EMAIL_PASS=your-app-password

# Required: signing secret for JWTs (use a long random string, e.g. openssl rand -hex 32)
# Never commit real values or reuse secrets across environments.
JWT_SECRET=your-secret-key-here
MONGODB_URI=mongodb://localhost:27017/career-path-explorer

# Comma-separated browser origins for CORS (set to your deployed SPA origin(s) in production)
# CORS_ORIGIN=https://your-app.example.com

# LLM Provider (used for job analysis / responsibility extraction)
# Supports any OpenAI-compatible API (OpenAI, Azure OpenAI, Ollama, etc.)
OPENAI_API_KEY=your-api-key-here
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
