import React from 'react';
import Simulation from './Simulation';

/** All simulation UX (run, evaluate, rank) lives at `/puzzle-job`. */
const SimulationResults = () => {
  return <Simulation />;
};

export default SimulationResults;
