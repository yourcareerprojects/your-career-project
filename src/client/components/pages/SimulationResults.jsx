import React from 'react';
import Simulation from './Simulation';

/** All simulation UX (run, evaluate, rank) lives at `/simulation/results`. */
const SimulationResults = () => {
  return <Simulation />;
};

export default SimulationResults;
